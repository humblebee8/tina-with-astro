{
  "name": "@example/blog",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "yarn tinacms server:start -c\"astro dev --port 3001\"",
    "start": "astro dev --port 3001",
    "build": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/renderer-preact": "^0.4.0",
    "astro": "^0.22.11"
  },
  "dependencies": {
    "@tinacms/cli": "^0.60.0",
    "styled-components": "^5.3.3",
    "tinacms": "^0.63.0"
  }
}